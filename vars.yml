---
- name: test playbook
  hosts: localhost
  tasks:
    - name: Git checkout
      ansible.builtin.git:
        repo: https://{{username}}:{{token}}@github.com/snehal/proj.git
        dest: /var/lib/awx/test/
