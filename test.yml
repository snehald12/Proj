---
- name: test playbook
  hosts: ip-10-0-128-135.ap-south-1.compute.internal
  collections:
     - amazon.aws
  tasks:

   - name: Gather information about all instances in AZ ap-southeast-2a
     amazon.aws.ec2_instance_info:
      region: ap-south-1
      filters:
          availability-zone: ap-south-1a
